def get_usage(user_id: int) -> int:
    return 0


def increment_usage(user_id: int) -> int:
    return 0


def get_remaining(user_id: int) -> int:
    return 0


def can_transcribe(user_id: int) -> bool:
    return True
